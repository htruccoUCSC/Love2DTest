name: Build LÖVE Game

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    name: Build bare love package
    uses: love-actions/love-actions-core@v1.1.1
    with:
      build-list: ./main.lua